/**
 * Copyright (c) {YEAR}
 * 
 * Declare Routes for Mongo Endpoints
 * 
 * @summary MongoRouter
 * @author {AUTHOR}
 * 
 * Created at       : {CREATED}
 * Last modified    : {MODIFIED}
 */
import { Router } from 'express';{logger-mddlwr-impl}
import MongoController from '../controllers/mongo.controller';

const router = Router();

router.get('/', {route-logging}MongoController.getAll);
router.get('/:id', {route-logging}MongoController.get);
router.post('/', {route-logging}MongoController.add);
router.put('/', {route-logging}MongoController.update);
router.delete('/:id', {route-logging}MongoController.delete);

export default router;
