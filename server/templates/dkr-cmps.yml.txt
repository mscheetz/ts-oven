version: "3"
networks:
    api_server_default:
        external: true

services:
    {APP-NAME}:
        networks:
            - api_server_default
        build:
            context: .
            dockerfile: dockerfile
        environment:
            ENV_NAME: dev
        ports:
            - 3000:3000
